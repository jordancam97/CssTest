import { Grid } from "@mui/material";
import React from "react";
import "./Header.css"

const Header = () => {
  return (
    <Grid className="header">
      <Grid className="header_text">AllRun</Grid>
    </Grid>
  );
};

export default Header;
